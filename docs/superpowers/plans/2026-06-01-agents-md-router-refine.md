# Plan: agents-md router refinement

- Files impacted: `AGENTS.md`
- Goal: keep the repo router lightweight while borrowing the clearer discovery/impact flow from `fcpx-auto-captions`.

## Steps

- [x] Re-read the current `AGENTS.md` and align the router sections with the local docs layout.
- [x] Apply a minimal patch that reduces duplication, keeps context intact, and makes the discovery order more explicit.
- [x] Verify the final `AGENTS.md` for readability and confirm no unrelated rules were removed.

## Verification

- [x] Manual reread of the edited file.
- [x] Compare the updated text against the requested scope: router clarity, duplicate cleanup, no context loss.
